name: "Data Operations Hub"

on:
  workflow_dispatch:
    inputs:
      action_type:
        description: 'Richtung wählen:'
        required: true
        default: 'to_editable'
        type: choice
        options:
          - to_editable
          - to_export

permissions:
  contents: write

jobs:
  run_op:
    runs-on: ubuntu-latest
    # DAS HIER SCHALTET DIE WARNUNG AB:
    env:
      FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true
      
    steps:
      - uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
      
      - name: Setup Python
        uses: actions/setup-python@v5 # Update auf v5 (bessere Node 24 Kompatibilität)
        with:
          python-version: '3.10'

      - name: Install Polars
        run: pip install polars pyarrow

      - name: Execute Sync
        run: python scripts/sync.py ${{ github.event.inputs.action_type }}

      - name: Commit Changes
        run: |
          git config --local user.email "hub-action@github.com"
          git config --local user.name "DataHub Bot"
          git add editable/ exports/
          git commit -m "Manual Sync: ${{ github.event.inputs.action_type }}" || echo "Nothing to commit"
          git push
